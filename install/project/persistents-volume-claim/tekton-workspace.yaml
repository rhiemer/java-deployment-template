apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-workspace
  labels:
    app: tekton
spec:
  storageClassName: gp2
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
