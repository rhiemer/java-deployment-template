apiVersion: triggers.tekton.dev/v1alpha1
kind: EventListener
metadata:
  name: java-deployment-template
spec:
  serviceAccountName: pipeline
  triggers:
    - bindings:
        - kind: ClusterTriggerBinding
          ref: github-push
      name: trigger-1
      template:
        ref: java-deployment-template